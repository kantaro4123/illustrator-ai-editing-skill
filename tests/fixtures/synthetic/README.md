# Synthetic Illustrator fixtures

The E2E suite generates all `.ai` documents at runtime under the operating-system
temporary directory. Fixtures contain synthetic Japanese text, live and outlined
aspect-ratio distortions, point and area text, locked content, RGB/CMYK documents,
similar names, and multiple artboards. No generated Illustrator or image artifact
may be committed.
