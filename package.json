{
  "name": "illustrator-ai-editing-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "illustrator-ai": "bin/illustrator-ai"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "validate": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
